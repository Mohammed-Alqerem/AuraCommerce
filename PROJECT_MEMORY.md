# Aura Commerce Project Memory

This is the durable technical handoff for future work on Aura Commerce. Read it at the start of a development session and update it in the same commit as every completed feature. User-visible capabilities belong in `FEATURES.md`; this file records engineering context, invariants, decisions, verification, and the next safe step.

## Feature delivery workflow

1. Start from an up-to-date `main` and create a `feature/<short-name>` branch.
2. Implement one coherent feature and its tests without mixing unrelated cleanup.
3. Update this file and `FEATURES.md` in the same feature commit.
4. Run the repository's Release build and tests. Generate and review SQL when the EF model changes.
5. Commit and push the feature branch; never push feature work directly to `main`.
6. Confirm GitHub Actions for the exact pushed commit. Merge only after the developer reviews that revision and explicitly approves the merge.

## Current handoff

- Date: 2026-08-25
- Working branch: `feature/google-apple-sign-in`
- Base: `feature/customer-support-profile-layout` at `8711ed5` (stacked feature branch)
- Current increment: real Google and Apple external authentication with provider-gated activation, verified customer onboarding, password-confirmed linking for existing emails, and a consistent login-page provider section
- State: implementation and local verification complete; push the exact revision, confirm remote CI, and obtain developer review before merging
- Recovery note: the expansion files found uncommitted on 2026-08-25 were confirmed as intentional work and are preserved in this feature branch

## Architecture snapshot

- ASP.NET Core MVC and Razor Views targeting .NET 9
- Entity Framework Core 9 with SQL Server in the application and in-memory SQLite in tests
- Custom session authentication using centralized session keys and persisted `Admin`/`Customer` roles
- ASP.NET Core remote authentication handlers use a short-lived, secure external cookie only for provider handshakes; the established application session remains the signed-in source of truth
- `IExternalAccountService` owns Google/Apple subject resolution, verified-email onboarding, and password-confirmed linking; provider tokens are never persisted
- `UserExternalLogin` stores one provider subject per customer/provider with database uniqueness for `(Provider, ProviderKey)` and `(UserId, Provider)`
- Controllers own HTTP concerns; `CheckoutService` owns transactional checkout orchestration
- `ISalesReportWorkbookExporter` owns the presentation-quality XLSX document boundary; the reports controller supplies filtered report data and returns the generated bytes
- ClosedXML 0.105.1 generates a single styled worksheet with typed dates/currency, formulas, tables, freeze panes, print settings, and safe text cells
- Admin routes that expose `_AdminNavigation.cshtml` keep it in the page header directly below the title/action row; `.admin-page-header` owns the shared spacing and responsive placement
- The shared store header exposes public/customer Support as a labeled Razor link while admin support remains in `_AdminNavigation.cshtml`; customers never enter controller or action names
- The store navbar expands only at Bootstrap's `xxl` breakpoint so its search and authenticated account controls cannot widen the document at intermediate desktop widths
- The profile form/sidebar uses an `xxl` 8/4 split, a two-card summary band from 768–1399px, 48px hero-to-content spacing, and zero-min-width/wrapping guards for long customer and order content
- EF configuration is centralized under `OnlineStore/Data/Configurations`; development seed data is under `OnlineStore/Data/Seed`
- GitHub Actions restores tools and packages, then performs a Release build and test run on pushes and pull requests

## Invariants to preserve

- Never determine authorization from a specific user ID.
- Never store a plaintext password, and never log, document, send, or render a plaintext password or stored password hash.
- Accept a return URL only when `Url.IsLocalUrl` confirms it is local.
- Never auto-link an external identity to an existing email; require a successful local customer password check, and never externally link or sign in an administrator.
- Require a verified provider email before creating or linking a customer; returning customers may sign in by their already-linked provider subject.
- Keep Google client secrets and Apple team/key/private-key material in user secrets, environment variables, or a deployment secret manager; never commit provider credentials or persist provider tokens.
- Scope carts, wishlists, notifications, tickets, and order queries to the signed-in customer.
- Calculate prices and totals from database products, not posted form values.
- Checkout must remain serializable, stock-aware, and atomic; stock cannot become negative.
- Preserve product name, unit price, shipping, delivery, and total snapshots for historical orders.
- Archive products and deactivate categories instead of destroying referenced history.
- A customer may have only one normalized email, one cart, one cart row per product, one review per product, and one wishlist row per product.
- New persisted timestamps use UTC.
- All unsafe MVC actions require anti-forgery validation.

## Database migration chain

Apply migrations in their committed order; do not rename a migration after it has been applied to a shared database.

1. `20260816101954_initialSetUpDataBase`
2. `20260817072658_UpdateProductImages`
3. `20260821162228_HardenAuthenticationAndDataIntegrity`
4. `20260824214838_StoreExpansion`
5. `20260824215133_AddStoreNotifications`
6. `20260824223654_BackfillLegacyOrderSnapshots`
7. `20260825124448_AddExternalAuthentication`

Back up databases containing real data and inspect generated SQL before applying schema changes. Production startup migrations remain opt-in through `Database:ApplyMigrationsOnStartup`.

## External provider and policy gates

- `IStoreEmailSender` is intentionally safe and unconfigured. Select and configure a transactional email provider before enabling real password-reset or email-verification delivery.
- Payments remain the existing manual flow. Do not add live payments until a provider and webhook/idempotency behavior are approved.
- Coupons, automatic cancellation, returns, and refunds require owner-approved pricing and eligibility policies before implementation.
- Connection strings and provider credentials belong in environment variables or deployment secrets, never committed configuration.
- Google activation requires OAuth credentials and an HTTPS `/signin-google` callback for every deployment.
- Apple activation requires an Apple Developer Services ID, Team ID, Key ID, PKCS #8 `.p8` secret, verified domain, and HTTPS `/signin-apple` return URL.

## Verification record

- 2026-08-25: Google/Apple external-account service tests passed for verified onboarding, returning subjects, no automatic existing-email linking, rejection of unverified email, password-confirmed matching-email linking, and administrator rejection; all 31 Release tests passed and the Release build completed with zero warnings.
- 2026-08-25: the `AddExternalAuthentication` idempotent SQL was generated and inspected; it creates only the provider-link table, cascade user foreign key, and the two intended unique indexes.
- 2026-08-25: DESIGN.md lint and the strict premium UI audit reported zero errors/findings, JavaScript syntax validation passed, `git diff --check` found no whitespace errors, and NuGet reported no known vulnerable direct or transitive packages.
- 2026-08-25: live HTTP smoke rendering returned 200 and confirmed both provider buttons, three anti-forgery tokens, the email divider, and disabled controls while credentials are absent. Desktop/narrow screenshot review could not run because no browser was connected; Razor compilation, strict UI audit, and responsive source checks passed.
- 2026-08-25: the final customer Support/profile-layout correction passed DESIGN.md lint with zero errors, the strict premium UI audit with zero findings, JavaScript syntax validation, and all 25 Release tests; `git diff --check` reported no whitespace errors.
- 2026-08-25: rendered desktop/narrow inspection for the corrected customer Support/profile layout could not run because no browser session was connected; responsive source inspection and Razor compilation passed, but screenshot comparison remains a review step.
- 2026-08-25: the admin header-navigation increment passed DESIGN.md lint with zero errors, the strict premium UI audit with zero findings, and all 25 Release tests.
- 2026-08-25: live desktop/narrow browser inspection for the admin header-navigation increment could not run because no browser session was connected; Razor compilation and responsive static checks passed, but a rendered screenshot comparison remains a review step.
- 2026-08-25: the Excel sales-report increment completed a Release build with zero warnings and all 25 tests passed, including workbook structure, types, formulas, tables, and formula-injection-safe product text.
- 2026-08-25: live browser inspection could not run because no browser session was connected; responsive layout rules and the repository's strict UI audit remain the non-browser verification path for this revision.
- 2026-08-25: the CI-equivalent Release build completed with zero warnings and all 25 tests passed.
- 2026-08-25: the Debug build completed with zero warnings.
- 2026-08-25: EF reported no pending model changes relative to the model snapshot.
- 2026-08-25: an idempotent SQL script through `BackfillLegacyOrderSnapshots` generated successfully at `OnlineStore/obj/store-expansion.sql` (ignored build output); the removed empty `test` migration is absent.

## Next-session checklist

1. Read this file and `FEATURES.md`.
2. Inspect `git status --short --branch`, the latest commits, and remote CI before editing.
3. Finish any item explicitly marked in progress before starting another feature.
4. Add the next requested capability to `FEATURES.md` and record its technical decisions here.
